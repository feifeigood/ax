---
name: emoji
description: Returns a corresponding emoji for a given word (e.g. happy, magic etc).
---

# Instructions
When the user asks for an emoji for a specific word or concept, use this skill.
1. Call `run_skill_script` to execute `emoji.sh`, passing the word as the first argument.
2. Return the output emoji directly to the user.
